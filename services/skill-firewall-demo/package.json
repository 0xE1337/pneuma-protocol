{
  "name": "@pneuma/service-skill-firewall-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Standalone demo skill that proves @pneuma/skill-firewall blocks 9 classes of malicious caller input. Zero on-chain dependency — runs without USDC / x402 / SkillRegistry.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "attack": "tsx scripts/run-attacks.ts",
    "demo": "tsx scripts/run-attacks.ts"
  },
  "dependencies": {
    "@pneuma/skill-firewall": "workspace:*",
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
